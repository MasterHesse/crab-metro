# crab-metro

基于 Rust 的简易地铁系统，支持读取 JSON 配置构建带权图，并通过命令行交互计算线路推荐与费用。

## 功能目标

- 读取地铁线路与计费规则配置（JSON）
- 启动时构建站点图；若无配置，生成默认配置
- 提供 shell 环境用于查询与管理
- 支持最短距离与最少换乘两种路线推荐
- 输出预计票价

## 快速开始

```bash
# 克隆项目后，运行
cargo run -- config/sample_config.json
```